#include<stdio.h>
void main()
{
         int a;
         printf ("Enter the no.");
         scanf("%d",&a);
         if(a%5==0)
          {
             printf("No.is Divisible by 5");
          }
         else
         {
               printf("Num is not Divisible by 5");
         }
}
